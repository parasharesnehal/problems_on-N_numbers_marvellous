// problems on string 

#include<stdio.h>

int main()
{
    char ch;

    printf("Enter a character: \n");
    scanf("%c", &ch);

    printf("the character enetered is: %c \n", ch); // %c is used to print character
    printf("the ASCII value of that character is : %d \n", ch); // %d is used to print ascii
    return 0;
}